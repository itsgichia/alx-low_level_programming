#include "main.h"
/**
 * mul -multiplies two integers
 * @a: first integer
 * @b: second integer
 *
 * Return: product of @a * @b
 */
int mul(int a, int b)
{
	int results = a * b;

	return (results);
}
